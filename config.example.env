# 🔧 Configuración del Sistema RAG Agent Crew
# Copia este archivo como .env y ajusta los valores según tu configuración

# ============================================
# 🧠 CONFIGURACIÓN DE OLLAMA (IA Local)
# ============================================
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b
EMBEDDING_MODEL=nomic-embed-text:latest

# ============================================
# 🔍 CONFIGURACIÓN DE QDRANT (Base Vectorial)
# ============================================
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=knowledge_base

# ============================================
# 🤖 CONFIGURACIÓN DE CREWAI
# ============================================
# Modelo principal para los agentes
MODEL=ollama/mistral:7b

# API base compatible con OpenAI para Ollama
API_BASE=http://localhost:11434/v1

# ============================================
# ⚙️ CONFIGURACIÓN AVANZADA (Opcional)
# ============================================
# Timeout para requests a Ollama (segundos)
OLLAMA_TIMEOUT=60

# Dimensiones del modelo de embeddings
EMBEDDING_DIMENSION=768

# Métrica de distancia para Qdrant (cosine, dot, euclidean)
DISTANCE_METRIC=cosine

# Score mínimo para búsquedas RAG (0.0 - 1.0)
MIN_SCORE_THRESHOLD=0.4

# ============================================
# 🚀 INSTRUCCIONES DE CONFIGURACIÓN
# ============================================
# 1. Instalar Ollama: curl -fsSL https://ollama.ai/install.sh | sh
# 2. Descargar modelos: ollama pull mistral:7b && ollama pull nomic-embed-text:latest
# 3. Iniciar Qdrant: podman run -d -p 6333:6333 --name qdrant qdrant/qdrant
# 4. Copiar este archivo: cp config.example.env .env
# 5. Ejecutar setup: crewai run setup 